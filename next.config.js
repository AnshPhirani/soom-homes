/** @type {import('next').NextConfig} */

// next.config.js
const nextConfig = {
  output: "export",
  //   images: { unoptimized: true },
  reactStrictMode: true,
  swcMinify: true,
  //   basePath: "/soom-homes",
};

module.exports = nextConfig;
